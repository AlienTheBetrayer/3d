# Only needed to run db:generate/db:migrate/db:push/db:pull/db:studio without going through @repo/api.
# Copy to packages/db/.env. Matches compose.dev.yml's defaults out of the box

DATABASE_DIRECT_URL="postgresql://fluxbound:dev-data@localhost:5434/fluxbound-dev"
